# Corpus

Status: **pending** (Phase O). Targets: 100+ distinct public assets across
sprites/sheets/UI/vector/tiled/natural/procedural/noise/normal-map/mesh/
animation/terrain classes plus negative controls, each with source URL,
upstream revision, SHA-256, class, exact license, attribution, and
redistribution/benchmark permissions. No redistribution → fetch manifest
only. Deterministic TRAIN/VALIDATION/TEST split (60/20/20 minimum) persisted
in the split manifest; negative controls included and expected to lose.
